db:
  host: mes-db
  port: 5432
  user: mes_user
  password: mes_password
  database: mes_db

api:
  base_url: "http://api:8000"
  api_key: "pat_cp1_xoxo"
  timeout_seconds: 5
  max_retries: 3
  backoff_seconds: 2

sync:
  mode: "full"
  batch_size: 500
  updated_at_column: "updated_at"
  state_file: "/app/.sync_state.json"

logging:
  level: "INFO"
  log_to_file: true
  file_path: "/app/connector.log"